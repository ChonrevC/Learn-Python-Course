# Data structure that can contain different values
# Similar to a list, but used for data that will never change
# Differences:
#   immutable (Can't be changed or modified)
#   Created with parantheses

coordinates = (4, 5)
print(coordinates[0])                                   # indexes are the same as a list

coordinates2 = [(4, 5), (6, 7), (80, 34)]               # a list of tuples
